package GameMenu;

import javafx.application.Application;

public class Main {
    public static void main(String[] args) {
        Menu menu = new Menu();
        Application.launch(menu.getClass());
    }
}
